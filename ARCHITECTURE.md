# Platform Design Policy

Status: Approved

## 1. 目的

shingo-cameraが提供する複数の撮影計画アプリを、共通の認証・購入・商品権限・管理画面で運用できる基盤を構築する。

初期対象はSUN AND MOON PLANNER、HANABI PLANNER、HANABI PLANNER Google Earth追加機能とする。

## 2. 前提規模

- 主な想定利用者数: 100〜300人
- 将来的な上限想定: 1,000人程度
- 運営者: 1人
- 管理画面: PC専用
- 例外対応: 手動対応を許容
- 自動化: 手動で処理しきれなくなってから追加

100万人向けの構成は採用しない。

## 3. 設計原則

### 3.1 シンプル

必要なテーブル、カラム、APIだけを持つ。

将来使うかもしれないという理由だけでは追加しない。

### 3.2 正本は一つ

同じ意味の情報を複数箇所で管理しない。

例:

- 認証情報の正本: Supabase Auth
- 商品名の正本: M_PRODUCT
- 商品権限の正本: T_USER_PRODUCT
- 購入履歴の正本: T_PURCHASE

### 3.3 認証と商品権限を分離

認証は「誰か」を確認する。

商品権限は「何を使えるか」を確認する。

商品を購入していないユーザーでも認証ユーザーは存在できる。

### 3.4 商品追加でDB変更しない

ユーザーごとの商品保有をM_USERのフラグ列では管理しない。

1ユーザー×1商品をT_USER_PRODUCTの1レコードとして管理する。

### 3.5 管理画面から復旧可能

以下は管理画面から実施できるようにする。

- ユーザー停止・再開
- 商品権限付与・停止・再開
- note移行状態確認
- note購入の手動紐付け
- ログ確認
- 要注意ユーザー確認

DB直接更新を通常運用にしない。

### 3.6 自動停止しない

不正利用の判定は運営支援と牽制を目的とする。

- システムはWarningを検知する
- 管理者へ通知する
- 最終判断は管理者が行う
- 永久停止は初期実装しない
- 誤検知時は速やかに再開できる

### 3.7 固定費を増やさない

必要な費用は許容する。

初期方針:

- ドメイン: 必要経費
- Cloudflare Workers Paid: 既存の必要経費
- Stripe: 固定費なし、売れた時の手数料は許容
- Supabase: 無料枠を基本
- その他SaaS: 既存構成で代替できない場合のみ採用

新規サービスを採用する場合は、以下を明記する。

- 目的
- 固定費
- 従量課金
- 既存構成での代替可否
- 採用しない場合の問題

### 3.8 既存アプリを壊さない

完成度の高いSUN AND MOON本体は、認証基盤のために大規模改修しない。

共通認証・権限確認・APIクライアントを追加し、アプリ固有の計算・描画は分離する。

### 3.9 将来のオフライン利用へ備える

初期リリースではオフライン機能を実装しない。

ただし以下を初期から考慮する。

- DEVICE_IDを発行する
- ログインログ・アクセスログへDEVICE_IDを残す
- 商品権限確認を共通処理にする
- 撮影計画1件をJSONとして扱える構造にする

将来はSUN AND MOONの保存済み撮影計画だけをスマホでオフライン利用可能にする。

Google Earthはオフライン対象外とする。

## 4. 運用方針

普段は管理画面を毎日確認しない。

要注意利用が検知された場合に管理者へメール通知し、必要な時だけ詳細確認する。

ログ件数が増えた場合は、CSV出力またはバックアップテーブルへ退避して削除する。

初期段階で高度なログ基盤、監視SaaS、AI自動判定は導入しない。

## 5. ドキュメント方針

- Gitを設計書の正本とする
- 設計が確定した時点でMarkdownへ反映する
- チャットの内容だけを正本にしない
- Claudeは実装前に本リポジトリを読む
- 仕様変更時は対応する文書を上書き更新する
- Git履歴を使うため、`AUTH_v2.md`のような複製は作らない
